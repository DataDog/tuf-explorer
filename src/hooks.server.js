// importing the directories module
// so that the app crashes at startup if env variables are not OK.
// note that this does not happen with the dev server,
// see https://github.com/sveltejs/kit/issues/927
import {} from "$lib/directories";
